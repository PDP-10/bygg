


/*
  Sequences:

  ^G    -- ring the bell (XBell(display, 0))
  ^H    -- cursor left
  ^I    -- tab
  ^J    -- cursor down
  ^M    -- goto left margin

  ESC [ (arg (;arg)) x:

        A       -- up (arg) lines
        B       -- down (arg) lines
        C       -- right (arg) chars
        D       -- left (arg) chars
        H       -- goto (arg1, arg2)
        J       -- erase to end of screen
        K       -- erase to end of line
*/
